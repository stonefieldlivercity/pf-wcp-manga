<%- if controller_name != 'sessions' %>
  既にアカウントをお持ちなら<%= link_to t('.sign_in'), new_session_path(resource_name) %>してください
<% end %>

<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  アカウントをお持ちでない場合、<%= link_to t('.sign_up'), new_registration_path(resource_name) %>してください
<% end %>



