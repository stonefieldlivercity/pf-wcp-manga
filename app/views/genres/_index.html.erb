<div class="row p-3">
  <div class="col p-0">
    <h5 class="text-center my-2">genres</h5>
    <% genres.each do |genre| %>
      <%= link_to genre.name, genre_path(genre) %>
    <% end %>
  </div>
</div>


