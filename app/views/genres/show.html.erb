<!--ジャンル絞り込みの作品一覧ページ-->
<h3 class="text-center p-3 mx-auto mt-n2 border">
  <%= t('.show_title') %> "<%= @genre.name %>"
</h3>
<div class="container px-0 my-2">
  <div class="row">
    <div class="col-12 mx-auto">
      <%= render 'books/index', books: @books %>
    </div>
  </div>
</div>